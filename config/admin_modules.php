<?php

/*
|--------------------------------------------------------------------------
| Admin module access (legacy reference)
|--------------------------------------------------------------------------
|
| Module permissions are stored in the database (`modules` + `module_role`).
| Default grants are seeded by ModuleSeeder. Manage live access from
| Settings → Permissions in the admin panel.
|
*/

return [];
